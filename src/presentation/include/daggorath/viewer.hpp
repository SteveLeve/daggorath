#pragma once
#include "daggorath/render_state.hpp"

namespace dag {

// VIEWER.ASM: architecture, creatures, peeks, vertical features, objects.
RenderState project_viewer(const ViewSnapshot& view);

}  // namespace dag
